<UserControl x:Class="PointlessWaymarks.WpfCommon.StarRating.StarRatingControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="24" d:DesignWidth="120">
    <UserControl.Resources>
        <Style x:Key="StarButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="FontSize" Value="18" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="MinWidth" Value="20" />
            <Setter Property="MinHeight" Value="20" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="Transparent" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
        <Button x:Name="Star1" Tag="1" Click="Star_Click" Style="{StaticResource StarButtonStyle}" />
        <Button x:Name="Star2" Tag="2" Click="Star_Click" Style="{StaticResource StarButtonStyle}" />
        <Button x:Name="Star3" Tag="3" Click="Star_Click" Style="{StaticResource StarButtonStyle}" />
        <Button x:Name="Star4" Tag="4" Click="Star_Click" Style="{StaticResource StarButtonStyle}" />
        <Button x:Name="Star5" Tag="5" Click="Star_Click" Style="{StaticResource StarButtonStyle}" />
    </StackPanel>
</UserControl>
